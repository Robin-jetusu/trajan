name: Vulnerable Workflow

on:
  pull_request_target:
    types: [opened, synchronize]

jobs:
  # CRITICAL: This loads workflow from PR branch (fork)
  build:
    uses: ./.github/workflows/build.yml
    secrets: inherit

  # Also vulnerable
  test:
    uses: ./workflows/test.yml
